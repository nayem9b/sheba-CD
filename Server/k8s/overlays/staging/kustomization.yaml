apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: staging

resources:
  - ../../base

images:
  - name: nayem9b/bookify-backend-github-build
    newTag: latest  

patches:
- path: deployment-patch.yaml
  target:
    kind: Deployment
    name: bookify-backend