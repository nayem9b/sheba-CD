apiVersion: v1
kind: Service
metadata:
  name: sheba-backend
  namespace: sheba
spec:
  selector:
    app: myapp
  ports:
  - port: 3000
    protocol: TCP
    targetPort: 3000
  - port: 4000
    protocol: TCP
    targetPort: 4000
  type: LoadBalancer
  
